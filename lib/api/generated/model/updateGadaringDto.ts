/**
 * Generated by orval v7.13.2 🍺
 * Do not edit manually.
 * Gada API
 * Backend API for the Gada event convening platform. Mobile app routes require a User JWT Bearer token (obtained via OTP auth). Admin routes require an Admin JWT Bearer token.
 * OpenAPI spec version: 0.1.0
 */
import type { UpdateGadaringDtoCategory } from './updateGadaringDtoCategory';
import type { UpdateGadaringDtoVisibility } from './updateGadaringDtoVisibility';
import type { UpdateGadaringDtoTicketKind } from './updateGadaringDtoTicketKind';

export interface UpdateGadaringDto {
  /** The public display name of the gadaring (event). */
  name?: string;
  /** A rich-text or plain description that gives attendees context about the event — what to expect, who it is for, etc. */
  description?: string;
  /** The category that best describes this gadaring. Used for discovery and filtering. */
  category?: UpdateGadaringDtoCategory;
  /** Controls who can see and join the event. PUBLIC events appear in discovery; PRIVATE events are invite-only. */
  visibility?: UpdateGadaringDtoVisibility;
  /** ISO 8601 datetime string for when the event begins. */
  startDateTime?: string;
  /** ISO 8601 datetime string for when the event ends. */
  endDateTime?: string;
  /** Full human-readable venue address shown to attendees. */
  address?: string;
  /** Latitude of the venue in decimal degrees. Used for geo-search and map pin display. */
  latitude?: number;
  /** Longitude of the venue in decimal degrees. Used for geo-search and map pin display. */
  longitude?: number;
  /**
   * Hard cap on the total number of attendees. Leave unset for unlimited attendance.
   * @minimum 1
   */
  maxParticipants?: number;
  /** Dress code instruction displayed on the event page and tickets (e.g. 'All-White', 'Smart Casual'). */
  dressCode?: string;
  /** Additional need-to-know information for attendees — parking tips, what to bring, etc. Displayed as an FYI note. */
  fyis?: string;
  /** Public URL of the event banner/cover image (stored on Cloudflare R2). Displayed in discovery cards and event detail. */
  bannerKey?: string;
  /** Determines the ticketing model. FREE = no payment required; PAID = attendees must purchase; MIXED = some free tiers, some paid. */
  ticketKind?: UpdateGadaringDtoTicketKind;
  /** Set to true to allow volunteers to apply for this event. Enables the volunteer tab on the event detail page. */
  volunteerEnabled?: boolean;
  /** ISO 8601 datetime for when push/email reminders should be sent to registered attendees. Defaults to 1 hour before startDateTime if omitted. */
  reminderTime?: string;
}
